{
  "name": "techx-cmms",
  "version": "0.1.0",
  "private": true,
  "description": "Tech X — small maintenance management app (assets, PM schedules, spare parts, work orders) with CSV import. Static, no build step.",
  "scripts": {
    "dev": "npx --yes serve . -l 3000",
    "start": "npx --yes serve . -l 3000"
  },
  "license": "MIT"
}
